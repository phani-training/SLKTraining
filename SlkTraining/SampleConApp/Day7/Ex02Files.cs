using System;
using System.IO;

//Static class called File that can be used to create/read Text files in UR Application
//Writes text to the file. 
//Append the contents

//Create a program that converts the objects of Employee class(Id, Name, Address) into a file of data as a CSV Document. 
namespace SampleConApp.Day7
{
    class Ex02Files
    {
        const string fileName = "test.txt";
        static void Main(string[] args)
        {
            //Console.WriteLine("Enter something to add");
            //string data = Console.ReadLine();
            ////File.WriteAllText(fileName, data);
            //File.AppendAllText(fileName, data);
            ////U cannot remove the contents directly..
        }
    }
}
